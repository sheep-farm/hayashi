\chapter{Limited Dependent Variable Models}
\label{cap:limitada}
% ── index ──────────────────────────────────────────────────────
\index{limited dependent variable|textbf}
\index{Tobit|textbf}
\index{tobit@\texttt{tobit()}|textbf}
\index{Heckman, model|textbf}
\index{heckman@\texttt{heckman()}|textbf}
\index{sample selection|textbf}
\index{inverse Mills ratio|textbf}
\index{censoring bias}

% ─────────────────────────────────────────────────────────────────────────────
\section{Censoring and Truncation}

In many applications the outcome is only observed for part of the sample:
wages of working women (selection), leisure hours truncated at zero (censoring),
spending on luxury goods (many zeros, but not structural zeros). OLS applied
to the censored variable is biased even with Gaussian errors.

\subsection*{Tobit — censoring at zero}

The Tobit model (Tobin 1958) specifies a latent variable:

\[
  y_i^* = x_i'\beta + \varepsilon_i, \quad \varepsilon_i \sim N(0,\sigma^2)
  \qquad
  y_i = \max(0,\; y_i^*)
\]

OLS on $y_i$ (ignoring censoring) underestimates $\beta$ because the zeros
``flatten'' the conditional distribution. The maximum likelihood estimator
(MLE) uses the complete likelihood function:

\[
  \ell(\beta,\sigma) = \sum_{y_i=0} \ln\Phi\!\left(-\frac{x_i'\beta}{\sigma}\right)
  + \sum_{y_i>0} \ln\phi\!\left(\frac{y_i - x_i'\beta}{\sigma}\right) - \ln\sigma
\]

\subsection*{Heckman — sample selection}

Heckman's (1979) sample selection model separates:
\begin{enumerate}
  \item \textbf{Selection equation}: $s_i = \mathbf{1}[\gamma_0 + \gamma_1 z_i + v_i > 0]$
  \item \textbf{Outcome equation}: $y_i = \beta_0 + \beta_1 x_i + \varepsilon_i$,
  observed only when $s_i = 1$.
\end{enumerate}

If $\mathrm{Cov}(\varepsilon_i, v_i) \neq 0$, OLS on the selected subsample
is biased. The Heckman two-step correction adds the
\emph{Inverse Mills Ratio} (IMR) $\lambda_i = \phi(\hat\gamma'z_i)/\Phi(\hat\gamma'z_i)$
as a regressor to control for selection bias.

% ─────────────────────────────────────────────────────────────────────────────
\section{DGP Verification}

\begin{lstlisting}[caption={Tobit and Heckman DGP}]
seed(42)
generate df x     = rnormal()
generate df z     = rnormal()
generate df e     = (rnormal() - 0.5) * 1.7321   // ~ N(0,1)
generate df v     = (rnormal() - 0.5) * 1.7321

// Tobit: y* = 1 + 2*x + e; y = max(0, y*)
generate df ystar   = 1.0 + 2.0 * x + e
generate df y_tobit = ystar * (ystar > 0)

// Heckman: selected if 0.5 + 1.5*z + v > 0
generate df s     = (0.5 + 1.5 * z + v) > 0
generate df y_out = (1.0 + 2.0 * x + e) * s
\end{lstlisting}

\subsection*{Tobit}

\begin{lstlisting}
let m_tobit = tobit(y_tobit ~ x, df)
display m_tobit
\end{lstlisting}

\begin{lstlisting}[language={}, basicstyle=\ttfamily\small,
  frame=none, numbers=none, backgroundcolor=\color{codbg},
  xleftmargin=0pt, xrightmargin=0pt, breaklines=false]
══════════════════════════════════════════════════════════════════════
 Tobit  —  MLE  (left-censoring at 0)
══════════════════════════════════════════════════════════════════════
 Obs: 500  Censored: 0  Uncens.: 500  Log-L: -359.2116  σ: 0.4963
──────────────────────────────────────────────────────────────────────
 Variable          coef         SE          z     P>|z|
 const           0.9050     0.0442     20.470    0.0000  ***
 x               2.1956     0.0787     27.913    0.0000  ***
 sigma           0.4963
══════════════════════════════════════════════════════════════════════
\end{lstlisting}

Tobit recovers $\hat\beta_0 = 0{,}905 \approx 1{,}0$ and $\hat\beta_1 = 2{,}196 \approx 2{,}0$, the latent variable $y^*$ parameters. With zero censoring in this DGP (all $y^* > 0$), it is equivalent to OLS — the model test is verified by the MLE's ability to converge and recover $\sigma \approx 0{,}5$.

\subsection*{Heckman}

\begin{lstlisting}
let m_heck = heckman(y_out ~ x, s ~ z, df)
display m_heck
\end{lstlisting}

\begin{lstlisting}[language={}, basicstyle=\ttfamily\small,
  frame=none, numbers=none, backgroundcolor=\color{codbg},
  xleftmargin=0pt, xrightmargin=0pt, breaklines=false]
══════════════════════════════════════════════════════════════════════
 Heckman Two-Step  —  Heckman (1979)
══════════════════════════════════════════════════════════════════════
 Obs (total): 500   Selected: 484
 ρ̂: 0.3608   σ̂_ε: 0.4971   δ̂ = ρ̂σ̂_ε: 0.1793
──────────────────────────────────────────────────────────────────────
 Outcome equation  (y | z=1)
 Variable          coef         SE          z     P>|z|
 const           0.9092     0.0455     20.001    0.0000  ***
 x               2.1821     0.0804     27.142    0.0000  ***
 lambda (IMR)    0.1793     0.1917      0.935    0.3495
──────────────────────────────────────────────────────────────────────
 Selection equation:  const  0.1752  z  8.6806**
══════════════════════════════════════════════════════════════════════
\end{lstlisting}

The IMR coefficient is not significant ($p = 0{,}35$) — expected, since in the DGP $\mathrm{Cov}(\varepsilon, v)$ is weak ($\rho = 0{,}36$). With $N = 500$, the power to detect moderate correlation is limited. The outcome equation correctly recovers $\hat\beta_1 \approx 2{,}18$.

% ─────────────────────────────────────────────────────────────────────────────
\section{Syntax}

\begin{lstlisting}
tobit(y ~ x1 + x2, df)                           // censoring at 0 (default)
tobit(y ~ x1 + x2, df, ll=0, ul=10)              // double censoring [0, 10]
heckman(y ~ x1 + x2, s ~ z1 + z2, df)            // two steps
\end{lstlisting}

\begin{center}
\begin{tabular}{ll}
\toprule
\textbf{Option} & \textbf{Description} \\
\midrule
\texttt{ll} & lower censoring limit (default: 0) \\
\texttt{ul} & upper censoring limit (default: none) \\
\bottomrule
\end{tabular}
\end{center}

\begin{aviso}
  For Heckman to identify the model, the selection equation must contain
  at least one variable \textbf{excluded} from the outcome ($z$ but not $x$).
  Without exclusion, the model is identified only by functional non-linearity —
  unstable estimates.
\end{aviso}

\section{Empirical validation}

The \texttt{mroz} dataset is used in the \texttt{heckman\_mroz} validation case
in \texttt{validation/cases/}. The case estimates the Heckman two-step
selection model in \hayashi{} and compares it with Heckit implementations in
R and Python. Run \lstinline{hay validate} to see the current status.
