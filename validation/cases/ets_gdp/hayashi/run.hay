// Load the US macro dataset from CSV.
load "validation/cases/ets_gdp/data/macrodata.csv" as df

// Simple exponential smoothing on US real GDP (matches ETS(A,N,N)).
let m = ses(df, gdp)

// Export ETS results for automated comparison.
export(m, "txt", "/dev/stdout")
