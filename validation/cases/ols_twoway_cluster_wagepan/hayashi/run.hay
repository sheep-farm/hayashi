// Load the Wooldridge wagepan dataset from CSV.
// The dataset is generated by the reference scripts before this script runs.
load "validation/cases/ols_twoway_cluster_wagepan/data/wagepan.csv" as df

// Estimate a pooled wage equation with worker and year clustered standard errors.
let m = ols(lwage ~ educ + exper + expersq + union + married, df, cluster=nr, cluster2=year)

// Export coefficient table for automated comparison.
export(m, "csv", "/dev/stdout")
