# bvar_simulated

Bayesian VAR is currently **not-supported** for numerical validation.

Reason: R mfbvar/bvar packages fail to install; PyMC implementation is too heavy for a deterministic CI reference.
