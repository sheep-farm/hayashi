# Zivot-Andrews test on a simulated random walk

The R reference generates a seeded random walk and writes the CSV consumed by
Hayashi. The oracle is `urca::ur.za`.

Compared quantity: the reported test statistic with tolerance `0.1`.
