load "validation/cases/dr_learner_simulated/data/data.csv" as df
let m = dr_learner(y ~ d + x, df, x="x")
let out = dataframe({
  "variable": ["ate"],
  "coef": [m.fit.ate],
  "std_err": [m.fit.ate_se]
})
let out2 = select(out, "variable", "coef", "std_err")
export(out2, "csv", "/dev/stdout")
