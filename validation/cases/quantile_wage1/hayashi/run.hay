// Load the Wooldridge wage1 dataset from CSV.
load "validation/cases/quantile_wage1/data/wage1.csv" as df

// Median wage regression.
let m = qreg(wage ~ educ + exper + tenure, df, q=0.5)

// Export coefficient table for automated comparison.
export(m, "txt", "/dev/stdout")
