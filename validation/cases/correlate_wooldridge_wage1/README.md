# Correlation matrix for Wooldridge wage1

Compares `correlate(df, wage, educ, exper, tenure)` against R and NumPy reference implementations.

Quantities compared: var1, var2, r, p (lower-triangular pairs).
