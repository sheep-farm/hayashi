// Load the Grunfeld dataset from CSV.
load "validation/cases/re_grunfeld/data/grunfeld.csv" as df

// Declare panel structure and estimate random-effects investment demand.
xtset(df, firm, year)
let m = re(inv ~ value + capital, df)

// Export coefficient table for automated comparison.
export(m, "txt", "/dev/stdout")
