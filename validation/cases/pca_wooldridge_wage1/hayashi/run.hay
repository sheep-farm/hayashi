// Load the public Wooldridge wage1 dataset generated by the reference scripts.
load "validation/cases/pca_wooldridge_wage1/data/wage1.csv" as df

// Hayashi standardises variables before PCA, matching prcomp(scale.=TRUE).
let m = pca(df, educ, exper, tenure, wage, n=2)
print(m)
