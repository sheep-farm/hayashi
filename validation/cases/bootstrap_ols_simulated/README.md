# bootstrap_ols_simulated

Bootstrap OLS on simulated data.

Notes: Simulated OLS data (y = 1 + 2x + noise). Hayashi bootstrap(ols, ...) is compared with R boot::boot and a Python statsmodels OLS pairs bootstrap; quantities are the mean and standard deviation of the bootstrap slope and intercept distributions.
