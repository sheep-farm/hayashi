// Load the Wooldridge wage1 dataset from CSV.
load "validation/cases/ci_wooldridge_wage1/data/wage1.csv" as df

// 95% confidence interval for the wage mean.
let c = ci(df, wage)

// Export the result as JSON for automated comparison.
export(c, "json", "/dev/stdout")
