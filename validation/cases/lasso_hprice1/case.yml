id: lasso-hprice1
title: Lasso regression on Wooldridge hprice1
estimator_family: lasso
status: blocked
dimension: numerical

dataset:
  name: wooldridge::hprice1
  source: R package wooldridge / Python wooldridge package
  licence: public teaching dataset

references:
  - R
  - Python

comparison:
  quantities:
    - coefficients
    - standard_errors
  tolerances:
    coefficients: 1e-2
    standard_errors: 1e-2

output_format: txt

hayashi_script: cases/lasso_hprice1/hayashi/run.hay
reference_scripts:
  R: cases/lasso_hprice1/reference/run.R
  Python: cases/lasso_hprice1/reference/run.py

notes: |
  Lasso regression of house price on lot size, square footage and bedrooms.
  Currently blocked: Hayashi's `lasso` prints the coefficient table to stdout
  but returns `Nil`, so `export(..., "txt", ...)` cannot be used for
  automated comparison. The estimator would need to return a proper result
  object for this case to become passable.

result:
  summary: "Blocked: Hayashi lasso returns Nil instead of a result object"
  known_differences: []
  issues_opened: []
