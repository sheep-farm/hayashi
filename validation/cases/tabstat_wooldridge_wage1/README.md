# Tabstat for Wooldridge wage1

Compares `tabstat(df, wage, educ, exper, tenure, stats=[...])` against R and NumPy reference implementations.

Quantities compared: variable, stat, value (long-format output).
