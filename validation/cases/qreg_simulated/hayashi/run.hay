load "validation/cases/qreg_simulated/data/data.csv" as df
let m = qreg(y ~ x, df, tau=0.75, boot=100)
let t = tidy(m)
let out = select(t, "variable", "coef", "std_err")
export(out, "csv", "/dev/stdout")
