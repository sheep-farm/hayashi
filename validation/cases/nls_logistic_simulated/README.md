# nls_logistic_simulated

Nonlinear least squares logistic model on simulated data.

Notes: Simulated data from y = a / (1 + exp(-b*(x-c))) + N(0, 0.2). Coefficients and standard errors compared against R `nls` and Python `scipy.optimize.curve_fit`.
