load "validation/cases/qrf_simulated/data/data.csv" as df
let m = qrf(y ~ x1 + x2, df, quantiles="0.75", trees=50, depth=5)
let nan = 0.0/0.0
let out = dataframe({
  "variable": ["r_squared"],
  "coef": [m.fit.oob_r_squared],
  "std_err": [nan]
})
let out2 = select(out, "variable", "coef", "std_err")
export(out2, "csv", "/dev/stdout")
