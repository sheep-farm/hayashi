% ============================================================
%  Hayashi — A Language for Applied Econometrics
%  Main file
% ============================================================
\documentclass[12pt, a4paper, twoside, openright]{book}

% ── Encoding & language ─────────────────────────────────────
% LuaLaTeX: fontspec substitui T1/inputenc
\usepackage{fontspec}
\usepackage[brazil]{babel}

% ── Typography ───────────────────────────────────────────────
\usepackage{microtype}
\usepackage{lmodern}
\usepackage{setspace}
\onehalfspacing

% ── Geometry ─────────────────────────────────────────────────
\usepackage[
  top=3cm, bottom=3cm,
  inner=3.5cm, outer=2.5cm,
  headheight=14pt
]{geometry}

% ── Colors ───────────────────────────────────────────────────
\usepackage{xcolor}
\definecolor{hayblue}{RGB}{0, 90, 160}
\definecolor{haygreen}{RGB}{0, 120, 80}
\definecolor{hayred}{RGB}{160, 30, 30}
\definecolor{codbg}{RGB}{245, 245, 245}
\definecolor{codframe}{RGB}{200, 200, 200}
\definecolor{comment}{RGB}{100, 120, 100}
\definecolor{keyword}{RGB}{0, 80, 160}
\definecolor{string}{RGB}{140, 40, 40}
\definecolor{number}{RGB}{60, 100, 60}

% ── Code listings ────────────────────────────────────────────
\usepackage{listings}

\lstdefinelanguage{Hayashi}{
  morekeywords={
    let, const, fn, return, if, else, for, in, while, break, continue,
    match, try, catch, and, or, not, true, false, nil,
    load, save, source, display, input, generate, mutate, filter,
    select, drop, rename, sort, dropna, append, collapse,
    group_by, pivot_longer, pivot_wider, summarize, codebook,
    tabulate, replace, keep, count, nrow, swilk, sfrancia, sktest
  },
  sensitive=true,
  morecomment=[l]{//},
  morecomment=[s]{/*}{*/},
  morestring=[b]",
  morestring=[b]',
  morestring=[s]{f"}{"},
}

\lstset{
  language=Hayashi,
  basicstyle=\ttfamily\small,
  keywordstyle=\color{keyword}\bfseries,
  stringstyle=\color{string},
  commentstyle=\color{comment}\itshape,
  numberstyle=\tiny\color{gray},
  numbers=left,
  numbersep=8pt,
  backgroundcolor=\color{codbg},
  frame=single,
  framerule=0.4pt,
  rulecolor=\color{codframe},
  breaklines=true,
  breakatwhitespace=false,
  showstringspaces=false,
  tabsize=4,
  captionpos=b,
  xleftmargin=1.5em,
  xrightmargin=0.5em,
  literate=
    {á}{{\'{a}}}1 {é}{{\'{e}}}1 {í}{{\'{i}}}1 {ó}{{\'{o}}}1 {ú}{{\'{u}}}1
    {Á}{{\'{A}}}1 {É}{{\'{E}}}1 {Í}{{\'{I}}}1 {Ó}{{\'{O}}}1 {Ú}{{\'{U}}}1
    {à}{{\`{a}}}1 {è}{{\`{e}}}1
    {ã}{{\~{a}}}1 {õ}{{\~{o}}}1 {Ã}{{\~{A}}}1 {Õ}{{\~{O}}}1
    {ç}{{\c{c}}}1 {Ç}{{\c{C}}}1
    {â}{{\^{a}}}1 {ê}{{\^{e}}}1 {ô}{{\^{o}}}1
    {ü}{{\"u}}1,
}

% estilo sem numeração para trechos inline curtos
\lstdefinestyle{inline}{
  numbers=none,
  frame=none,
  backgroundcolor=\color{codbg},
  basicstyle=\ttfamily\small,
}

% ── Tables ───────────────────────────────────────────────────
\usepackage{booktabs}
\usepackage{tabularx}

% ── Graphics ─────────────────────────────────────────────────
\usepackage{graphicx}

% ── Environments ─────────────────────────────────────────────
\usepackage{tcolorbox}
\tcbuselibrary{most}

\newtcolorbox{nota}{
  colback=hayblue!6,
  colframe=hayblue!60,
  fonttitle=\bfseries,
  title=Nota,
  left=6pt, right=6pt, top=4pt, bottom=4pt,
}

\newtcolorbox{aviso}{
  colback=hayred!6,
  colframe=hayred!60,
  fonttitle=\bfseries,
  title=Atenção,
  left=6pt, right=6pt, top=4pt, bottom=4pt,
}

\newtcolorbox{dica}{
  colback=haygreen!6,
  colframe=haygreen!60,
  fonttitle=\bfseries,
  title=Dica,
  left=6pt, right=6pt, top=4pt, bottom=4pt,
}

% ── Headers & footers ────────────────────────────────────────
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\fancyhead[LE]{\small\textit{\leftmark}}
\fancyhead[RO]{\small\textit{\rightmark}}
\fancyfoot[C]{\small\thepage}
\renewcommand{\headrulewidth}{0.4pt}

% ── Hyperlinks ───────────────────────────────────────────────
\usepackage[
  colorlinks=true,
  linkcolor=hayblue,
  urlcolor=hayblue,
  citecolor=hayblue,
  pdftitle={Hayashi — A Language for Applied Econometrics},
  pdfauthor={Flávio de Vasconcellos Corrêa},
  pdfsubject={Econometrics, Programming Language},
]{hyperref}

% ── Custom commands ──────────────────────────────────────────
\newcommand{\hay}{\texttt{hay}}
\newcommand{\hayashi}{\textsc{Hayashi}}
\newcommand{\version}{0.2}

% ============================================================
\begin{document}

% ── Capa ────────────────────────────────────────────────────
\begin{titlepage}
  \centering
  \vspace*{3cm}
  {\Huge\bfseries\color{hayblue} Hayashi \par}
  \vspace{0.5cm}
  {\large\itshape A Language for Applied Econometrics \par}
  \vspace{2cm}
  {\large Flávio de Vasconcellos Corrêa \par}
  \vspace{1cm}
  {\normalsize Versão \version{} \par}
  \vfill
  {\small \today \par}
\end{titlepage}

% ── Página em branco ────────────────────────────────────────
\cleardoublepage

% ── Frontmatter ─────────────────────────────────────────────
\frontmatter
\include{chapters/00_prefacio}

\tableofcontents
\cleardoublepage

% ── Parte I: A Linguagem ─────────────────────────────────────
\mainmatter

\part{A Linguagem}

\include{chapters/01_introducao}
\include{chapters/02_instalacao}
\include{chapters/03_repl}
\include{chapters/04_tipos_valores}
\include{chapters/05_variaveis_escopos}
\include{chapters/06_operadores}
\include{chapters/07_controle_fluxo}
\include{chapters/08_lacos}
\include{chapters/09_funcoes}
\include{chapters/10_erros}
\include{chapters/11_strings}
\include{chapters/12_listas_dicionarios}
\include{chapters/13_dataframes}
\include{chapters/14_manipulacao}
\include{chapters/15_pipes}
\include{chapters/16_modulos}

% ── Parte II: Econometria Aplicada ───────────────────────────
\part{Econometria Aplicada}

\include{chapters/17_ols}
\include{chapters/18_iv}

% ── Backmatter ───────────────────────────────────────────────
\backmatter
\include{chapters/apendice_a_instalacao_avancada}
\include{chapters/apendice_b_referencia_rapida}

\end{document}
